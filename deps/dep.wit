package abc:xyz;

interface x {
   record person {
      name: string,
      age: u32,
   }
}

world dep {
   use x.{person};

   record when {
      time: u64,
   }

   export f: func(who: list<person>, when: option<when>) -> string;
}